<?php

use Mmd\Game\Form\ServerEditForm;

$title = $this->translate('Добавить сервер');
$this->headTitle($title);

/** @var ServerEditForm $form */
$form = $this->form;
$form->setAttribute('action', $this->url('admin/server', ['action' => 'create']));
$form->prepare();

?>

<div class="col-md-4 col-md-offset-4">
    <?php if (!empty($this->success)) : ?>
        <div class="alert alert-success"><?php echo $this->success; ?></div>
    <?php endif; ?>
    <?php echo $this->adminServerEditForm($form); ?>
</div>